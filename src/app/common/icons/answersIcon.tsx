import { BsPatchExclamationFill } from "react-icons/bs";

const AnswerIcon = ({ ...props }) => {
  return <BsPatchExclamationFill className={`text-2xl ${props.className}`} />;
};

export default AnswerIcon;
